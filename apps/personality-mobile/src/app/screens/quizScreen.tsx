import { Text } from 'react-native';
import React from 'react';
import { KContainer } from '@teamway-test/ui-kit';

const QuizScreen = () => {
  return (
    <KContainer>
      <Text>QuizScreen</Text>
    </KContainer>
  );
};

export default QuizScreen;
